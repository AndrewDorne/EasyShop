%h2 Your Shopping Cart
%table
  = render(cart.line_items)

.row
  .span3.offset9
    %h3 Total #{number_to_currency(cart.total_price)}
    = button_to "Checkout", "#", :class => "btn btn-large btn-success"
